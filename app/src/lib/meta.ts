/** Métadonnées de l'application affichées dans les paramètres. */

/** Version de l'application (alignée sur `package.json` / crate `accord-app`). */
export const APP_VERSION = '0.1.0';

/** Licence du projet. */
export const APP_LICENSE = 'MIT';

/** Fichier listant les licences des dépendances tierces (racine du dépôt). */
export const THIRD_PARTY_FILE = 'THIRD_PARTY.md';
