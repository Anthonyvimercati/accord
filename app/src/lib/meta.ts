/** Métadonnées de l'application affichées dans les paramètres. */

/** Version de l'application, injectée depuis `package.json` au build (voir vite.config.ts). */
export const APP_VERSION = __APP_VERSION__;

/** Licence du projet. */
export const APP_LICENSE = 'MIT';

/** Fichier listant les licences des dépendances tierces (docs/ du dépôt). */
export const THIRD_PARTY_FILE = 'docs/THIRD_PARTY.md';
