/**
 * Constante injectée par Vite au build (voir `vite.config.ts` → `define`),
 * égale à la version de `package.json`. Consommée par `src/lib/meta.ts` pour
 * afficher le numéro de version dans les réglages sans le maintenir à la main.
 */
declare const __APP_VERSION__: string;
